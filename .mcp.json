{
  "mcpServers": {
    "prozessia-tools": {
      "command": "bash",
      "args": [
        "-c",
        "cd \"$(dirname \"$0\")/backend\" && exec .venv/bin/python -m app.mcp_server",
        "${CLAUDE_PROJECT_DIR}/.mcp.json"
      ]
    }
  }
}
